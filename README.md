# Arizona Civil Dashboard

Панель для учёта лидеров гражданских структур Arizona RP.

## Установка в Termux

```bash
pkg update -y
pkg install nodejs -y
cd ~/arizona-civil
npm install
npm start
```

Открой в браузере: http://localhost:3000

База SQLite создаётся автоматически в `arizona.db`.
